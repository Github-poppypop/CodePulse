# CodePulse Setup

- Install: `npm install`
- Typecheck: `npm run typecheck`
- Lint: `npm run lint`
- Build: `npm run build`

MCP servers are configured via Hermes config (`config.yaml`), not this project.
